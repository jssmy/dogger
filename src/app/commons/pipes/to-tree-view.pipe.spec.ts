import { ToTreeViewPipe } from './to-tree-view.pipe';

describe('ToTreeViewPipe', () => {
  it('create an instance', () => {
    const pipe = new ToTreeViewPipe();
    expect(pipe).toBeTruthy();
  });
});
